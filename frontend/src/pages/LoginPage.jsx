const LoginPage = () => {
  const handleLogin = (e) => {
    e.preventDefault();
  }

  return (
    <div>
      <h1>Login Page</h1>
      <p>Welcome to the Login Page</p>
    </div>
  );
};

export default LoginPage;